{
  "name": "model_parcel_sass",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "serve": "parcel src/index.html --no-cache",
    "createBuild": "mkdir ./build",
    "buildProject": "parcel build src/index.html --dist-dir build",
    "createStatic": "mkdir ./build/static",
    "copieFiles": "cp -r ./src/static ./build",
    "build": "npm run createBuild && npm run buildProject && npm run createStatic && npm run copieFiles"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.12.0",
    "parcel": "^2.12.0",
    "sass": "^1.77.6"
  },
  "dependencies": {
    "animate.css": "^4.1.1",
    "granim": "^2.0.0",
    "locomotive-scroll": "^5.0.0-beta.21",
    "wowjs": "^1.1.3"
  }
}
